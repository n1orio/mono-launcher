# Змены

## [2.0.0-alpha.9] — 2026-09-13

### Інтэрфейс
- Замена глобальнага `updateInfo` на па-пакетны `updateInfoByPack` (`Record<string, UpdateInfo>`) — плашкі абнаўленняў і кнопкі прывязаныя толькі да актыўнай зборкі
- Перапісаны `refreshModUpdates` з па-пакетным захаваннем стану і хешаваньнем усталяваных версій — больш няма stale-даных пры пераключэнні зборак
- Выправлена `TabPlay.vue`: `customModsFiles` цяпер выкарыстоўвае `watch` на `status.value?.custom_mods` для мгненнай рэактыўнасці, выправлена адлюстраваньне зялёнай плашкі на ўсіх зборках
- Дададзены `currentPackUpdate` computed і `handleUpdate` выкарыстоўвае яго замест `updateInfo.value`
- Дададзены `packBackendMeta` — сінхранізуецца з `meta` бэкэнду; выкарыстоўваецца для праверкі `use_authlib`
- Выправлена `syncPackWithBackend`: `packBackendMeta` скиданне пры памылцы/пустатэ, remote versions карэктна адстэжваюцца
- Выправлена `load()` `checkForUpdates`: цяпер абіраецца апошняя версія паводле `created_at`, а не радкоўнага сартыраваньні `version`
- NeoForge JPMS выпадзенне выправлена: выключаны толькі `neoforged/neoforge/` (рэальны мод NeoForge) і `net/minecraft/client/` (srg-клент) з classpath — fmlloader/fmlcore/bus/securejarhandler ДОЛЖНЫ заставацца на classpath (яны рэгіструюць forgeclient праз ServiceLoader). Ванільны client jar (`1.21.1.jar`) выключаны з NeoForge `-cp` цалкам — ModLauncher сам знаходзіць патчаны кліент праз "production client provider"; даданне яго стварае дублікат модуля `_1._21._1`. Таксама дададзены `${version_name}`/`${version}` у placeholders для карэктнай падстановкі `--version` з `version.json`. Дададзена поўнае логаванне каманды запуску Java
- Выправлена ўсталёўка старэйшай версіі: `resolve_latest_url` полагаўся на `ORDER BY version DESC` у базе, але радкавая сартыроўка не працуе для semver ("1.10" < "1.2"). Цяпер версіі сартуюцца па `created_at DESC` у Rust. Дададзены fallback праз `backend_id` і `mono_pack_detail` для збірок з каталогу, дададзеных праз прамую спасылку
- Выправлена адпраджэнне спісу ўсталяваных версій (старая першай): `mrpack::installed_details()` сартаваў `InstalledVersion` па `version_id` па ўзрастанні. Зменена на памяншэнне, каб паказвалася найсвежэйшая ўсталяваная версія
- Выправлена завісанне пры абнаўленні зборкі: `extract_mrpack` меў `let mut total: u64 = 0`, што перакрываў `let total = archive.len()`. `file_total`/`total`/`current` у `DownloadProgress` паказвалі байты цячэння файлу замест агульнай колькасці/байтаў, што прыводзіла да таго, што інтэрфейс паказваў 100% пакуль распалоўка працягвалася. `file_total` цяпер лічыць толькі файлы (без дырэкторый), фінальны прагрэс 100% заўсёды эмуецца пасля цыклу. Таксама `handleInstall` і `installRemoteVersion` цяпер збрашчваюць `progress.value = null` у `finally`, каб віджэт загрузкі закрыўся

## [2.0.0-alpha.7] — 2026-09-06

### Бібліятэка
- Адзіная разгорнутая плашка катэгорыі з попіксельным выраўноўваннем сеткі.
- Перапрацоўка сеткі бібліятэкі (паліроўка картак течак).

### Інтэрфейс
- Адзіны кампанент `AppIcon` (замяняе 184 інлайн-SVG).
- Палепшаны markdown-рэдактар; дададзены адсутныя ключы перакладу.
- Палепшаны панэль загрузак, налады сеткі, бясконцая пракрутка каталога.
- Паліроўка наладаў / гульні / каталога; фікс CurseForge CDN.

### CurseForge
- **Фільтр па загрузчыку** — пошук модпакаў CurseForge фільтруе па `modLoaderType` (forge / fabric / quilt / neoforge) замест `categoryId`.
- **Аўтафільтры** — адкрыццё зборкі аўтаматычна ўжывае фільтры са статусу пака.
- **Поўныя апісанні** — карткі модпакаў паказваюць поўны HTML (prose-стылі) замест кароткага summary.
- **Бясконцая пракрутка** вынікаў пошуку.

### Кантэкстныя меню
- ПКМ па зборцы ў сайдбары → Гуляць / Налады / Навіны / Выдаліць.
- ПКМ па файле ў спісе → Дэталі / Абнавіць / Укл-Выкл / Выдаліць / Адкрыць тэчку.
- Шматразовыя кампаненты `AppContextMenu` і `AppContextMenuItem`.

### Тулбар
- Меню «...» з секцыямі: кіраванне модамі, аперацыі з файламі, выдаленне.
- Кнопка выдалення перанесена ў выпадашку (чырвоная іконка).

### Інтэрфейс
- Маштаб шрыфта: 13px → 14px.
- Памер акна: 1200×750, мінімум 950×650.
- Хелпер `enableAllFiles()` для масавых аперацый укл/выкл.

### Выпраўленні
- Адноўлены адсутныя файлы кампанентаў (tabs/, modals/).
- Версія паднята да 2.0.0-alpha.7 ва ўсіх канфігах (package.json, tauri.conf.json, Cargo.toml, лакалі).

## [1.3.1] — 2026-08-18

### Моды і сховішча
- Новая ўкладка **«Дублікаты»** скануе `mods/`, `resourcepacks/` і `shaderpacks/` і групуе файлы з аднолькавым змесцівам (па памеры + SHA-1). Паказвае, колькі месца можна вызваліць, дазваляе выдаліць адзін файл або пакінуць па адным у групе.

### Скрыншоты
- Скрыншоты цяпер паказваюць **дату і час** здымка — бейджам на кожнай плітцы і ў поўнаэкранным праглядзе.

## [1.3.0] — 2026-08-18

### Бібліятэка
- Новая ўкладка **«Бібліятэка»**: усе ўсталяваныя зборкі, згрупаваныя па катэгорыях, пліткі з маштабаваннем.
- Кожная плітка паказвае актыўны модлаадар/версію або «не ўсталявана»; катэгорыі можна хаваць.
- Новы раздзел **«Нядаўнія»** у бакавой панэлі паказвае вашы апошнія запушчаныя зборкі (да 10).

### Брэндынг зборак
- **Перайменаванне зборкі** проста з інтэрфейсу — для убудаваных зборак імя захоўваецца на экзэмпляр, для карыстацкіх абнаўляецца `packs.json`.
- **Загрузка свайго банэра** (рэдагаванне іконкі ўжо было) — захоўваецца лакальна для кожнай зборкі.

### Моды і абнаўленні
- Усталяваныя ўручную моды цяпер паказваюць сапраўдныя метаданыя — іконку Modrinth/CurseForge, назву праекта і версію — замест сырых назваў файлаў.
- **«Абнавіць усё» цяпер па ўкладках** (моды / рэсурспакі / шэйдары), з лічыльнікам.
- Пошук Modrinth атрымаў **фільтр тыпу версіі** (Рэліз/Бэта/Альфа), фільтр загрузчыка, «Загрузіць яшчэ» і «Скінуць фільтры».
- «Завіслыя» запісы трэкаваных модаў (файлы ўжо выдалены з дыска) чысцяцца, таму значок абнаўлення больш не паказвае прывіднае абнаўленне.

### Геймплэй і інтэрфейс
- Пакуль гульня запушчана, кнопка «Гуляць» ператвараецца ў чырвоную **«Спыніць»**, якая завяршае працэс, з карэктным выхадам (без ілжывага «гульня ўпала» пры наўмыснай спыненні).
- Асноўная панэль інтэрфейсу цяпер **па цэнтры** экрана, а не расцягваецца ад краю да краю.

### Пад капотам
- Новы персістэнтны **HTTP-кеш** (умоўныя запыты ETag/304), каб паўторныя запускі лаўнчэра не цягнулі сетку без змен.
- Пашыраныя Rust-юніт-тэсты; праверка тыпаў фронтэнду.